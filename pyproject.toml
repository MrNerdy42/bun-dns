[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bun-dns"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "requests"
]

[project.scripts]
bun-dns = "bun_dns.main:main"

[tool.setuptools]
packages = ["bun-dns"]